apiVersion: v1
kind: Service
metadata:
  name: contextslo
  namespace: contextslo-system
spec:
  selector:
    app.kubernetes.io/name: contextslo
  ports:
    - name: http
      port: 8080
      targetPort: http
